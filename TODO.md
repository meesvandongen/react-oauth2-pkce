1. Allow multiple instances of AuthCore to authenticate automatically (via global locking mechanism? spread across multiple tabs (i.e. local storage?)?).
2. middleware for react-router
3. react component for protecting routes
4. plugin system for modifying requests, actions upon events, etc.
5. handling of opaque tokens
6. metadata/discovery endpoint caching
7. Create a list of guarantees provided by the library. e.g. you will always have a valid access token when isAuthenticated is true.

